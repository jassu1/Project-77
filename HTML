<html>
    <head>
        <title>C-77</title>
    </head>
    <body>
        <h1>
            open console
        </h1>
        <script>
            console.log("Hi I am console");

            var firstname="Jasvitha";
            console.log(firstname);
            
            var lastname="Puttagunta";
            console.log(lastname);

            var fullname=firstname.concat(lastname);
            console.log(fullname);

            var length_fullname=fullname.length;
            console.log(length_fullname);

            var name1="I am Jhon Gihstdre";
            console.log(name1);

            var replacename=name1.replace("Gihstdre","Cina");
            console.log(replacename);

            var capital_name="JASVITHA";
            var makinglowercase=capital_name.toLowerCase();
            console.log(makinglowercase);

            var makinguppercase=makinglowercase.toUpperCase();
            console.log(makinguppercase);

            var squareroot=Math.sqrt(25);
            console.log(squareroot);

            var max_number=Math.max(56,85);
            console.log(max_number);

            var min_number=Math.min(45,72);
            console.log(min_number);

            var power_ex_1=Math.pow(4,2);
            console.log(power_ex_1);

            var power_ex_2=Math.pow(5,3);
            console.log(power_ex_2);
        </script>
    </body>
</html>